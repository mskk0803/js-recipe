const genkiFunction = function(number) {
  for (let n = 2; n <= number; n++) {
    if (n % 3 === 0) {
      console.log(n + "!!!!!!!")
    } else {
      console.log(n)
    }
  }
}

const FizzBuzz = function(num){
  for (let x = 1; x <= num n++){
    if (x % 3 === 0) {
      console.log(x + "FIzz")
    }else if (x % 5 === 0 ){
      console.log(x + "Buzz")
    }else {
      console.log(x)
    }
  }
}